/* let age: number = 20;
let existsFlag: boolean = true;
let language: string = 'JavaScript'; */

// 自动类型推断
let age = 20;
let existsFlag = true;
let language = 'JavaScript'

// 声明类型，但没有赋值
let favoriteLanguage: string;
console.log(language);


// tsc xxx.ts  => xxx.js  编译ts文件为js文件

// ts-node xxx.ts  直接运行,不生产js文件